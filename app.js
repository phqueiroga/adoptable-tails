import { makeSrcdoc } from "./src/code-validator.js";
const API=window.APP_CONFIG?.apiBase||"http://localhost:3000/api/runs",form=document.querySelector("#brief-form"),steps=[...document.querySelectorAll(".step")],agents=[["Atlas","Researcher"],["Mosaic","Designer"],["Forge","Maker"],["Beacon","Communicator"],["Compass","Manager"]];let step=0;
function showStep(i){step=i;steps.forEach((el,n)=>el.classList.toggle("active",n===i));document.querySelector("#step-label").textContent=`${["Context","Outcome","Boundaries"][i]} · ${i+1} of 3`;document.querySelector("#bar").style.width=`${(i+1)*33.34}%`;document.querySelector("#back").hidden=i===0;document.querySelector("#next").hidden=i===2;document.querySelector("#submit").hidden=i!==2;document.querySelector("#form-error").hidden=true}
const valid=()=>[...steps[step].querySelectorAll("input,textarea,select")].every(c=>c.checkValidity());document.querySelector("#next").onclick=()=>valid()?showStep(step+1):document.querySelector("#form-error").hidden=false;document.querySelector("#back").onclick=()=>showStep(step-1);
function pipeline(status){const at=({queued:0,researching:0,designing:1,building:2,communicating:3,reviewing:4,approved:5,revision_required:5,rejected:5})[status]??0;document.querySelector("#pipeline").innerHTML=agents.map(([name,role],i)=>`<li class="${i<at?"done":i===at?"active":""}"><b>${i+1}</b><strong>${name}</strong><br>${role}</li>`).join("");if(at<5){document.querySelector("#working-title").textContent=`${agents[at][0]} is ${["researching the destination","designing the experience","building the webpage","planning visitor engagement","reviewing the operation"][at]}.`;document.querySelector("#working-copy").textContent=["Wikidata must be queried live before the first handoff.","Exactly one product will be selected and specified.","HTML, CSS and JavaScript are being created and validated.","Messages, touchpoints and measurable actions are being prepared.","Evidence, function, accessibility and business value are being checked."][at]}}
const wait=ms=>new Promise(r=>setTimeout(r,ms));async function request(id){const res=await fetch(`${API}?run_id=${encodeURIComponent(id)}`),data=await res.json();if(!res.ok)throw new Error(data.error||"REQUEST_FAILED");return data}async function poll(id){for(let i=0;i<110;i++){const run=await request(id);pipeline(run.status);if(["approved","revision_required","rejected","failed"].includes(run.status))return run;await wait(3000)}throw new Error("PIPELINE_TIMEOUT")}
async function advance(id){for(let i=0;i<5;i++){let run=await request(id);pipeline(run.status);if(["approved","revision_required","rejected","failed"].includes(run.status))return run;const res=await fetch(API,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"advance",run_id:id})}),data=await res.json();if(!data.run_id)throw new Error(data.error||"ADVANCE_FAILED")}return request(id)}
function esc(v=""){const e=document.createElement("span");e.textContent=String(v);return e.innerHTML}function render(run){document.querySelector("#working").hidden=true;const root=document.querySelector("#result");root.hidden=false;if(run.status==="failed"){root.innerHTML=`<div class="error-card"><p class="kicker">Run ${esc(run.id)}</p><h2>The pipeline stopped safely.</h2><p>${esc(run.error)}</p><button class="primary" onclick="location.reload()">Start again</button></div>`;return}const m=run.outputs.manager,maker=run.outputs.maker,comm=run.outputs.communicator,approved=run.status==="approved";root.innerHTML=`<div class="result-head"><span class="status">Compass · ${esc(run.status.replace("_"," "))}</span><p class="kicker">${esc(maker.product_type.replaceAll("_"," "))}</p><h2>${esc(maker.product_title)}</h2><p>${esc(m.executive_summary)}</p></div>${approved?'<iframe class="experience-frame" title="Experience created by Forge" sandbox="allow-scripts"></iframe>':'<div class="error-card"><h2>Not launch-ready</h2><p>Compass requires changes before launch.</p></div>'}<div class="evidence-grid"><article class="panel"><h3>Live research</h3><p>${esc(run.tool_call.result.result_count)} Wikidata entities</p><ul>${run.outputs.researcher.evidence_items.slice(0,5).map(e=>`<li><a href="${esc(e.source_url)}" target="_blank" rel="noopener">${esc(e.label)}</a> — ${esc(e.relevance)}</li>`).join("")}</ul></article><article class="panel"><h3>Launch strategy</h3><p>${esc(comm.value_proposition)}</p><ul>${comm.visitor_touchpoints.slice(0,4).map(x=>`<li>${esc(x)}</li>`).join("")}</ul></article></div><details><summary>Inspect all five validated handoffs</summary>${run.handoffs.map(h=>`<div class="handoff"><strong>${esc(h.from)} → ${esc(h.to)}</strong> · validated</div>`).join("")}</details><p><small>Run ID: ${esc(run.id)} · Prompt: ${esc(run.prompt_version)}</small></p>`;if(approved)root.querySelector("iframe").srcdoc=makeSrcdoc(maker.files);root.scrollIntoView({behavior:"smooth"})}
form.onsubmit=async e=>{e.preventDefault();if(!valid())return void(document.querySelector("#form-error").hidden=false);const briefing=Object.fromEntries(new FormData(form));briefing.desired_duration_minutes=Number(briefing.desired_duration_minutes);document.querySelector("#briefing").hidden=true;document.querySelector("#working").hidden=false;pipeline("queued");try{const res=await fetch(API,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({briefing})}),data=await res.json();if(!data.run_id)throw new Error(data.details?.join(", ")||data.error);sessionStorage.setItem("journeyFoundryRun",data.run_id);history.replaceState(null,"",`?run_id=${encodeURIComponent(data.run_id)}`);const run=await advance(data.run_id);sessionStorage.removeItem("journeyFoundryRun");render(run)}catch(err){render({id:"not-created",status:"failed",error:err.message})}};
const shared=new URLSearchParams(location.search).get("run_id"),pending=shared||sessionStorage.getItem("journeyFoundryRun");if(pending){document.querySelector("#briefing").hidden=true;document.querySelector("#working").hidden=false;pipeline("queued");(shared?request(pending):advance(pending)).then(render).catch(err=>render({id:pending,status:"failed",error:err.message}))}
