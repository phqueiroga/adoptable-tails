# AI Usage Log

## Current state (as of 2026-08-16)

- Five agents (Researcher, Designer, Maker, Communicator, Manager) run on Claude Haiku 4.5 with strict structured-output contracts, orchestrated by Vercel Functions and driven interactively with Claude Code (Sonnet 5).
- The Researcher's only external source is Google Places; Open-Meteo, Google Routes and Anthropic web search were all evaluated at earlier stages and are not used in the current pipeline.
- The Maker outputs original HTML/CSS/JavaScript. Deterministic sanitisation, JSON-schema validation, a `jsdom`-based functional test that plays the page like a visitor, CSP and iframe sandboxing are all applied before display — the platform, not the Maker, owns the mission and reward-unlock mechanics.
- Live simulations across many varied attractions (a Berlin techno club, a Kyoto Zen garden, an Ibiza nightclub, a family bakery, a Vienna coffeehouse, museums and heritage sites) produced approvals, justified revision decisions and concrete failure/remediation evidence, most of it captured as regression test fixtures.

Record material AI-assisted work here for academic transparency. Review and correct every output before using it in the submission.

| Date | Tool/model | Task | Prompt summary | Output used | Human review or changes |
|---|---|---|---|---|---|
| 2026-08-09 | Codex | Project setup | Continue prior planning and create the initial repository foundation | README, security defaults, architecture outline, usage-log template | Pending student review |
| 2026-08-11 | Codex | Supabase fallback | Activate the live synthetic-data plan after no RescueGroups response | Database schema, RLS policy, 40 fictional records, questionnaire, deterministic matching rules and tests | Pending student review |
| 2026-08-11 | Codex | Agent architecture | Evaluate the implementation against the assignment brief and implement a cumulative five-agent Edge Function | Five distinct system prompts and schemas, live Researcher tool call, evidence records, grade-risk analysis | Pending student review |
| 2026-08-11 | Claude Haiku 4.5 + Codex | Pipeline iteration | Run the complete pipeline after configuring Claude; diagnose truncation and semantic interpretation errors | Increased structured-output allowance; added explicit alone-time semantics, preference boundaries, and score-order validation | Pending student review |
| 2026-08-16 | Claude Code (Sonnet 5) | Bug diagnosis and pipeline fixes | Investigate a failed run and a systemic Researcher photo bug; fix run-ID casing, the never-populated `hero_photo` field, and a resume path that skipped research on retry | Root-caused via direct data inspection rather than guessing; two earlier hypotheses (Google quota, result ranking) were tested and discarded before finding the real cause (`callStructured()` never returned `tool_calls`) | Verified against live run data before and after each fix |
| 2026-08-16 | Claude Code (Sonnet 5) | Platform-owned mission/reward mechanics | Move mission answering, hint/reveal, progress and reward unlock/lock entirely out of Maker-generated code into deterministic platform JS/CSS, after repeated live failures where agent-authored mechanics silently didn't work | `src/code-validator.js` mission engine and CSS gates, `src/contracts.js` handoff checks | Regression-tested against real captured HTML/CSS/JS from failing runs (`test/real-run-regression.test.js`) |
| 2026-08-16 | Claude Code (Sonnet 5) | Functional testing | Build a `jsdom`-based test (`src/page-functional-test.js`) that loads each generated page, clicks every control like a visitor, and plays all four missions, catching non-functional buttons, invisible content, premature reward visibility and unanchored overlays | Runs automatically as part of the Manager stage on every pipeline run, and as a standalone test suite | Deliberately documents jsdom's own limitations (no layout engine, no CSS-cascade specificity) rather than overclaiming coverage |
| 2026-08-16 | Claude Code (Sonnet 5) | Designer visual identity, reward badge, campaign recommendation | Add a Designer-owned per-attraction palette/typography (contrast- and installed-font-validated), a platform-rendered completion badge, an optional honestly-hedged in-person note, and a Designer-proposed real-world campaign idea kept out of the delivered visitor link and folded into the Communicator's client email instead | Schema, prompt and platform-rendering changes across `agents/definitions.js`, `src/code-validator.js`, `app.js` | Iterated across several live runs based on direct visual feedback (contrast, alignment, duplicate copy, hero image proportions) |
| 2026-08-16 | Claude Code (Sonnet 5) | Codebase cleanup for submission | Remove decommissioned dev-time evaluation scripts and stale architecture docs referencing an unimplemented Anthropic web-search capability; correct `README.md`, `docs/project-decisions.md` and `.env.example` to match the actual current implementation | This log entry, plus the file removals and edits themselves | Cross-checked every claim removed or added against the live agent prompts and `process.env` usage in the code, not against memory |
